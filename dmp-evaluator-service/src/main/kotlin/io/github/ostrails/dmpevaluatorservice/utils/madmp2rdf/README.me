# How to build
Go to the rmlmapper-java directory and execute:
```
mvn install -DskipTests=true
```

# How to execute
In the root project directory `madmp2rd` run:
```
java -jar rmlmapper-java/target/rmlmapper-7.3.3-r374-all.jar -m rmlmappings/rmlMappings.ttl -o madmps_rdf/ex9-dmp-long.ttl -s turtle
```